
FILE(GLOB SRCS *.h *.cpp)
ADD_LIBRARY(vtl_new_boman SHARED ${SRCS})

FIND_PACKAGE(ZLIB)
IF(ZLIB_FOUND)
    add_definitions(-DUSE_ZLIB)
    INCLUDE_DIRECTORIES( ${ZLIB_INCLUDE_DIR} )
    TARGET_LINK_LIBRARIES(vtl_new_boman ${ZLIB_LIBRARY} )
ENDIF()

