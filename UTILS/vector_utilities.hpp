void fill_double_vector_with_zeros(double *vec,size_t size){
    for(size_t I = 0 ; I < size ; I ++)
        vec[I] = 0.0;
}
void fill_unchar_vector_with_zeros(unsigned char *vec,size_t size){
    for(size_t I = 0 ; I < size ; I ++)
        vec[I] = 0;
}
